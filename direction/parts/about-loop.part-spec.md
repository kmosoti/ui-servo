# Part: about-loop

Read `_conventions.md` first. The one purely prose part: why observability,
why now. Entirely static — no tags, no disclosure, no JS.

Content, from the prototype's about section, in this order:

1. The Lego paragraph ("What I actually enjoy is the fitting-together part —
   more Lego than art project…").
2. The pullquote: *Building the automation is the easy half. Knowing whether
   it's working is the actual job.* — set with an accent left rule, larger
   and bolder than body text but still the text face (there is only one
   face). This is the ONE permitted pullquote on the whole site; the hero's
   thesis line stays conversational because this one carries the weight.
3. The tech-debt paragraph ("Skip that second half and tech debt moves in
   quietly…").
4. The loop motif: `build → observe → improve` as an inline row of dot-marked
   nodes, with the repeat line "↺ repeat, ideally faster each time" under it.
   Static markup — the dots do not pulse, the arrows do not animate.
5. The agentic paragraph ("It's also why agentic engineering is the obvious
   next branch, not a pivot…").
6. The working-style list, three items, each prefixed by an accent `→`:
   boring over clever; configuration separate from behaviour; logs and
   failure notes are evidence, not noise.

Rules:

- Measure respects the contract's density bounds (54–78ch); this part is
  where the "reads like a well-kept page" claim is proven or lost.
- No boxes. This part sits directly on the page background; the pullquote's
  rule and the accent arrows are its only ornament.
