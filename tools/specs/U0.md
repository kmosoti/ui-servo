# U0 — Foundation: README, ARCHITECTURE, direction contract, domain/contract.py

Owned paths (touch nothing else): `README.md`, `ARCHITECTURE.md`, `direction/`, `ui_servo/__init__.py`, `ui_servo/domain/__init__.py`, `ui_servo/domain/contract.py`, `pyproject.toml`, `tests/test_contract.py`, `tests/test_architecture.py`, `REVIEW_LOG.md` (create empty with header).

## Deliverables

1. **README.md** (rewrite; current one is tier-based and stale): ui-servo = Gauntlet Loop (Shumer, July 2026: agent-led decomposition → isolated builder subagents → blind, rotated, fresh-context critics judging side-by-side against a real reference bar → gap-driven revision → integration pass; invariants: implementer never self-grades, critics context-blind + rotated, bar is concrete, human is the brake) fused with a tiered feedback loop (latency classes: 0 pre-ship deterministic gates, 1 in-browser probe, 2 shadow-browser harness, 3 async cross-family model panel — deterministic tiers prune before any model spends tokens; no model in the hot path) and taste machinery (direction contract as the bar, blandness metric, MAP-Elites quality-diversity, correctness ⊥ taste, Goodhart guards). Purpose: build the owner's personal website (axum + htmx + WASM islands in site/). Cross-family critics ride locally-authenticated CLIs (claude/codex/agy) — no API keys.
2. **ARCHITECTURE.md**: hexagonal ports & adapters with a systems-thinking mapping — direction contract = reference signal; probe + shadow browser = sensors; gates + critics = comparators; builders = actuators; evidence = stock, findings = flow; fast homeostatic loop (`control/regulator.py`, deterministic correctness) vs slow exploratory loop (`control/critique.py` + `control/explore.py`, taste); Conant–Ashby: the regulator's internal model IS direction contract + exemplars; requisite variety via cross-family panel + QD sampling; Goodhart governance = multi-axis rubric + human sampling. Dependency rule: `domain` imports only stdlib (+pydantic); `control` → `domain` + `ports` only; `adapters` implement `ports`. Include the package tree.
3. **direction/direction.toml** v1 — the versioned reference signal for a distinctive personal site (NOT generic SaaS): `[meta]` version/name; `[palette]` LCH-space colors (background, surface, text, accent, accent-2 — pick a confident non-default scheme, e.g. deep warm dark + electric accent); `[type]` scale (ratio, base px, families: one distinctive display face + one text face, system-stack fallbacks); `[spacing]` scale (base, ratio, steps); `[motion]` allowed durations ms, easings (cubic-bezier strings), animatable properties (transform, opacity only), reduced-motion policy required; `[density]` axis bounds; `[references]` named entries (e.g. linear.app precision, rauno.me restraint, brutalist type sites) and `[anti_references]` (default shadcn, Bootstrap, stock SaaS landing).
4. **ui_servo/domain/contract.py** — pure domain (stdlib + pydantic only): `DirectionContract` pydantic model parsing direction.toml (tomllib); derivations: `to_css_custom_properties() -> str` (tokens.css content, `--color-*`, `--space-*`, `--type-*`, `--motion-*`; LCH via `oklch()` CSS), `motion_table() -> MotionTable` (allowed durations/easings/properties for the probe + gates), `class_allowlist_seed() -> frozenset[str]` (utility class names derived from tokens). CLI: `python -m ui_servo.domain.contract --check` (parse + round-trip + print summary), `--emit-css <path>`. Python 3.14 robust features: PEP 695 type aliases, frozen dataclasses or pydantic frozen models, match statements where natural.
5. **pyproject.toml**: keep existing deps; ensure hatch packages config matches `ui_servo`; add nothing heavy.
6. **tests/test_contract.py**: parse v1 file, round-trip, CSS emission contains expected custom properties, motion table matches toml. **tests/test_architecture.py**: import-linter-style check via stdlib (walk ui_servo modules' import statements with ast; assert domain imports no ui_servo.ports/adapters/control; control imports no adapters) — must pass trivially now and guard future waves.

## Acceptance (run these; all must pass)

- `cd ~/src/ui-servo && uv sync`
- `uv run python -m ui_servo.domain.contract --check`
- `uv run python -m ui_servo.domain.contract --emit-css /tmp/tokens-check.css && grep -q -- '--color' /tmp/tokens-check.css`
- `uv run pytest -q`

## Conventions

Python 3.14, uv-managed. Fully type-annotated (PEP 695). No I/O in domain except the CLI entrypoint guarded under `if __name__ == "__main__"` (file reading for the CLI lives in the `__main__` block, not in the model API — the model takes parsed dicts or toml strings). Docstrings explain the systems-thinking role of each abstraction. No comments narrating implementation.
