# U11 — Slow loop, exploration: MAP-Elites archive + frontier report

Owned paths: `ui_servo/domain/variant.py` (ADD the archive to the U10 module — coordinate: only the MAP-Elites classes; do not alter StyleVector/Variant APIs), `ui_servo/control/explore.py`, `tests/test_explore.py`. READ: U10 regulator report shape, U1 exemplar store port, U9 panel outcome shape.

## Deliverables

1. **domain/variant.py additions**: `StyleAxes` (density, expressiveness, motion_intensity — each derived deterministically from the StyleVector/motion evidence; document derivations), `EliteArchive` — MAP-Elites grid (3×3×3 default): `place(variant, axes, fitness)` keeps the fittest per cell (fitness = panel rank within direction + distinctiveness bonus, gates already passed by construction — the archive REFUSES variants whose RegulatorReport.passed is False), `frontier() -> list[Elite]`, coverage stats.
2. **control/explore.py**: `seed_cells(k)` → distinct target cells for K builder briefs (max spread); `admit(regulator_report, panel_rank)` → archive placement; `frontier_report(archive, out_html)` → self-contained HTML (inline screenshots base64, per-cell cards: screenshot, axes, gate summary, panel findings, blandness) for the human pick; `record_pick(elite, exemplar_store)` → exemplar saved (html + screenshot + meta) — the ONLY path that writes exemplars, because taste injection is a human act.
3. **tests**: archive keeps fittest per cell, refuses gate-failed variants; seed_cells(4) returns 4 distinct maximally-spread cells; frontier report renders from fixture data (valid HTML containing the cards); record_pick writes exemplar via a tmp store.

## Acceptance

- `uv run pytest tests/test_explore.py -q`
- `uv run pytest tests/test_architecture.py -q`

Do not git commit.
