# U5 — Shadow-browser sensor: ports/sensor.py + playwright adapter

Owned paths: `ui_servo/ports/sensor.py`, `ui_servo/adapters/playwright_sensor.py`, `ui_servo/adapters/vendor/axe.min.js` (vendored at build — download pinned version, record version+sha in a comment), `tests/test_playwright_sensor.py`, `tests/fixtures/sensor/` (fixture pages incl. seeded contrast violation + baseline/regression pair). READ: U0 contract, U1 evidence/store.

## Deliverables

1. **ports/sensor.py** (shape amended by orchestrator ruling to the stronger typed contract): frozen `PixelDiff` / `TraceSummary` value objects; `SensorReport` (span_id, turn_id, url, screenshots: Mapping[str, Path] with a non-colliding namespace for the full-page entry, aria_snapshot: str, axe_violations: Sequence[Mapping], pixel_diff: PixelDiff | None, trace: TraceSummary | None, reduced_motion_ok: bool, signals: Sequence[Signal]); `SensorPort` Protocol `observe(url, *, span_id, turn_id, baseline: Path|None, interactions|None) -> SensorReport`; a StubSensor test double in the port module proving the constructor contract.
2. **playwright_sensor.py** (sync playwright API is fine): navigate; full-page + per-`[data-span-id]` screenshots; `page.accessibility.snapshot()`-equivalent via aria snapshot (`page.locator('body').aria_snapshot()` in modern playwright); inject vendored axe-core, run with wcag2aa tags, collect violations verbatim; pixel diff vs baseline via Pillow (ImageChops, diff ratio + heatmap png); CDP tracing around a configurable interaction script (dropped frames from `Graphics.*`/trace events is brittle — implement via `page.evaluate` rAF-gap sampler during interactions: count frame gaps > 32ms as dropped; document the choice); reduced-motion check: reload with `page.emulate_media(reduced_motion="reduce")`, assert `document.getAnimations()` durations are 0/none → `reduced_motion_ok`. Everything also emitted as `Signal`s (source "harness") for the evidence store.
3. **tests**: fixture with seeded contrast violation → axe reports it; baseline vs regressed fixture → diff ratio > threshold and heatmap written; reduced-motion fixture honoring the media query → ok=True; one not honoring → ok=False.

## Acceptance

- `uv run pytest tests/test_playwright_sensor.py -q`
- `uv run pytest tests/test_architecture.py -q`

Do not git commit.
