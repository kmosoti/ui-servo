# U7 — WASM island pipeline: site/islands/

Owned paths: `site/islands/` (cargo crate), `site/assets/islands/` (built artifacts), edits to `site/src/` ONLY to mount the island on the home page + serve its assets, `tests/test_island.py`. READ: U6 site, U3 probe conventions (probe/README.md event names).

## Deliverables

1. **site/islands/**: wasm-bindgen crate (`crate-type = ["cdylib"]`) defining one custom element `<ui-constellation>` (an interactive project-constellation placeholder: a canvas with a few animated nodes reacting to pointer; modest, token-colored via CSS custom properties read from getComputedStyle). Build with `wasm-pack build --target web --out-dir ../assets/islands` (add wasm32 target via rustup if missing). A JS loader shim (`site/assets/islands/loader.js`) registers the element, wires `connectedCallback` try/catch to dispatch `ui-servo:ce-error`, sets a panic hook dispatching `ui-servo:wasm-panic`, and wraps instantiate rejections per probe README conventions.
2. Mount on `/` inside a fragment with `data-span-id`; respect `prefers-reduced-motion` (static render, no animation loop).
3. A deliberate test hook: query param `?panic=1` on the element attribute triggers a Rust panic (proves the sensor chain end-to-end).
4. **tests/test_island.py**: Playwright against `cargo run` server (build first): island custom element upgrades and draws (canvas non-blank via pixel sample); with `?panic=1` a `ui-servo:wasm-panic`-derived beacon event lands (serve through preview server shell OR assert the CustomEvent fires via page.evaluate listener — document which).

## Acceptance

- `wasm-pack build` succeeds; `cd site && cargo build` still clean
- `uv run pytest tests/test_island.py -q`

Do not git commit.
